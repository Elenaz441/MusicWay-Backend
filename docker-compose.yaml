version: "3.9"

services:
  db:
    image: postgres:12-alpine
    container_name: db-sing
    restart: always
    ports:
      - "5433:5432"
    environment:
      - POSTGRES_DB=$POSTGRES_DB
      - POSTGRES_USER=$POSTGRES_USER
      - POSTGRES_PASSWORD=$POSTGRES_PASSWORD
    networks:
      - fastapiappnetwork

  adminer:
    image: adminer
    container_name: adminer-sing
    restart: always
    ports:
      - "1235:8080"
    networks:
      - fastapiappnetwork

  app:
    build: .
    container_name: app-singing
    restart: always
    ports:
      - "8001:8000"
    env_file:
      - /src/.env
    depends_on:
      - db
    networks:
      - fastapiappnetwork

networks:
  fastapiappnetwork: